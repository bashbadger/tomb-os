[package]
name = "tomb_core"
version = "1.0.0"
edition = "2021"
authors = ["Tomb OS Engineering Team"]
description = "High-performance memory-safe zero-trust microkernel engine for Tomb OS"

[dependencies]
zeroize = "1.7"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
